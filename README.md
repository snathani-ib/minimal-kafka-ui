# minimalistic-kafka-ui
Super simple stripped down kafka UI

To run local kafka:  
    make setup-local-kafka

To run:  
    pip install -r requirements.txt  
    python3 app.py  
    Access the app on localhost:5000

